<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="help">

	<resultMap type="inquiry" id="inquiryResult">
		<id property="iNo" column="i_no"/>
		<result property="iType" column="i_type"/>
		<result property="iTitle" column="i_title"/>
		<result property="iContent" column="i_content"/>
		<result property="iDate" column="i_date"/>
		<result property="status" column="status"/>
		<result property="iWriter" column="i_writer"/>
		<result property="aContent" column="a_content"/>
	</resultMap>
	
	<resultMap type="faq" id="faqResult">
		<id property="fNo" column="f_no"/>
		<result property="fTitle" column="f_title"/>
		<result property="fContent" column="f_content"/>
		<result property="status" column="status"/>
	</resultMap>

	<insert id="insertInquiry" parameterType="inquiry">
		insert into inquiry values(SEQ_I_NO.NEXTVAL,#{iType},#{iTitle},#{iContent},default,default,#{iWriter},null)
	</insert>
	
	<select id="inquiryList" resultMap="inquiryResult" parameterType="String">
		select * from inquiry where i_writer = #{id} order by i_date desc
	</select>
	
	<select id="selectCount" resultType="_int" parameterType="String">
		select count(*) from inquiry where i_writer = #{id}
	</select>
	
	<select id="inquiryView" parameterType="_int" resultMap="inquiryResult">
		select * from inquiry where i_no=#{no}
	</select>
	
	<insert id="insertFaq" parameterType="faq">
		insert into faq values(SEQ_F_NO.NEXTVAL,#{fTitle},#{fContent},default)
	</insert>
	
	<select id="faqView" resultMap="faqResult">
		select * from faq order by f_no desc
	</select>
	
	<select id="selectFaqCountOne" resultType="_int">
		select count(*) from faq
	</select>
	
</mapper>
